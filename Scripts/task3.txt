project/
       db/
       srv/
       package.json
the script for the same is below=

{
  "name": "c_cpe_template",
  "version": "1.0.0",
  "description": "Course Template",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "9.6.1",
    "@sap/xssec": "4.12.1",
    "express": "4.22.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.1.2",
    "@sap/cds-dk": "9.6.0"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": {
        "[production]": {
          "kind": "hana"
        }
      },
      "auth": {
        "kind": "dummy"
      }
    }
  },
  "private": true
}


here we have to set the auth as none as instructed in the task book=
auth:"none"


project/
       db/
       srv/
       package.json
       mta.yamal
the script for the same is below=

parameters:
  instances: 1
  buildpack: nodejs_buildpack
  app-name: ${space}-c_cpe_submission-srv
  routes:
    - route: ${org}-cpe-submission.${default-domain}




_schema-version: 3.3.0
ID: c_cpe_template
version: 1.0.0
description: "Course Template"

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production

modules:
  - name: c_cpe_template-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
      app-name: ${space}-c_cpe_submission-srv
      routes:
        - route: ${org}-cpe-submission.${default-domain}
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: c_cpe_template-db

  - name: c_cpe_template-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: c_cpe_template-db

resources:
  - name: c_cpe_template-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared




The initiallizing of the MTA=
npm install -g mbt
mbt build
mta_archives/*.mtar